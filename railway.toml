[build]
builder = "NIXPACKS"

[deploy]
startCommand = "npx prisma migrate deploy && node scripts/ensure-admin.mjs && npm start"
healthcheckPath = "/login"
healthcheckTimeout = 60
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
